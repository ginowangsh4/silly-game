interface IDisplay {

    // update the sum field 
    void sumDisplay(int s);

    // update the message field 
    void msgDisplay(String s);

    // update the skip counter field
    void skipDisplay(int s);
}
